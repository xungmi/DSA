package sort.heap;

/*
	Các bước chính:
		1/ Xây dựng Max-Heap từ mảng ban đầu
		2/ Đưa phần tử lớn nhất (gốc heap) ra cuối mảng.
		3/ Giảm kích thước heap, và heapify lại để đảm bảo tính chất max-heap.
		4/ Lặp lại cho tới khi còn 1 phần tử.

	Max Heap:
		Là cây nhị phân hoàn chỉnh:
			Tất cả các mức (trừ mức cuối cùng) đều đầy đủ node
			Các node của mức cuối cùng nằm từ trái qua phải.
			Giá trị của node cha ≥ giá trị của 2 node con


 */
public class Heap {

}
